import { CloseDropDownsDirective } from './close-drop-downs.directive';

describe('CloseDropDownsDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseDropDownsDirective();
    expect(directive).toBeTruthy();
  });
});
